import * as React from 'react';
import './styles.css';

class Header extends React.Component{
    render(){

        return(
            <header className="top-header"> 
            <h1 id="header-heading">Memrise</h1>
            </header>
        );
    }
}

 export default Header;
